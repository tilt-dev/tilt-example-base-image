version: 2.1
jobs:
  build:
    docker:
      - image: tiltdev/tilt:latest

    steps:
      - checkout
      - setup_remote_docker
      - run: ctlptl create cluster kind --registry=ctlptl-registry && tilt ci
